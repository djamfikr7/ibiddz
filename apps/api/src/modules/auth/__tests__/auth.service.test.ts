import { AuthService } from '../auth.service';

describe('AuthService', () => {
  describe('normalizePhone', () => {
    it('should normalize Algerian phone numbers', () => {
      // AuthService should handle phone normalization
      expect(AuthService).toBeDefined();
    });
  });
});
